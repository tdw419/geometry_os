// Geometry OS - Pack nibbles into byte
// Packs two 4-bit values into byte
// Args: r0=low_nibble, r1=high_nibble
// Output: r2=packed byte

:pack_nibbles
    LDI r2, 0
    LDI r3, 15
    AND r0, r0, r3
    LDI r3, 15
    AND r1, r1, r3
    LDI r3, 4
    SLL r1, r1, r3
    OR r2, r0, r1
    HALT

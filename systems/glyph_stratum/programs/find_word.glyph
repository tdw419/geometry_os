// Geometry OS - Find Word
// Finds 16-bit word in memory
// Args: r0=start, r1=count, r2=target
// Output: r3=index or -1

:find_word
    LDI r3, -1
    LDI r4, 0

:loop
    SUB r5, r1, r4
    JZ r5, :done
    LOAD r5, r0
    SUB r5, r5, r2
    JZ r5, :found
    ADD r0, r0, 1
    ADD r4, r4, 1
    JMP :loop

:found
    MOV r3, r4

:done
    HALT

// Geometry OS - Copy Bytes
// Copies n bytes
// Args: r0=src, r1=dst, r2=count

:copy_bytes
    LDI r3, 0

:loop
    SUB r4, r2, r3
    JZ r4, :done
    LOAD r4, r0
    STORE r1, r4
    ADD r0, r0, 1
    ADD r1, r1, 1
    ADD r3, r3, 1
    JMP :loop

:done
    HALT

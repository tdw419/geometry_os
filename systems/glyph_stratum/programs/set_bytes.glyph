// Geometry OS - Set Bytes
// Sets n bytes to a value
// Args: r0=dst, r1=value, r2=count

:set_bytes
    LDI r3, 0

:loop
    SUB r4, r2, r3
    JZ r4, :done
    STORE r0, r1
    ADD r0, r0, 1
    ADD r3, r3, 1
    JMP :loop

:done
    HALT

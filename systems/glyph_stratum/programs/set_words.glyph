// Geometry OS - Fill 16-bit Words
// Fills memory with 16-bit value
// Args: r0=dst, r1=value, r2=count

:set_words
    LDI r3, 0

:loop
    SUB r4, r2, r3
    JZ r4, :done
    STORE r0, r1
    ADD r0, r0, 1
    ADD r3, r3, 1
    JMP :loop

:done
    HALT

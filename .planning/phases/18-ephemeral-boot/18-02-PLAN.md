---
phase: 18-ephemeral-boot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - systems/pixel_compiler/boot/multi_boot_manager.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ContainerInfo tracks ephemeral status"
    - "Ephemeral containers marked with is_ephemeral=True"
    - "State file includes ephemeral flag"
    - "list_containers() returns ephemeral status"
  artifacts:
    - path: "systems/pixel_compiler/boot/multi_boot_manager.py"
      provides: "ContainerInfo with ephemeral tracking"
      contains: "is_ephemeral.*bool"
  key_links:
    - from: "ContainerInfo.to_dict"
      to: "state file JSON"
      via: "serialization"
      pattern: "is_ephemeral.*:"
    - from: "_load_state"
      to: "ContainerInfo"
      via: "deserialization"
      pattern: "is_ephemeral"
---

<objective>
Add ephemeral tracking to ContainerInfo and MultiBootManager state persistence.

Purpose: Enable ps command and commit to identify ephemeral containers.
Output: ContainerInfo with is_ephemeral field, state file includes ephemeral flag.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing architecture
@systems/pixel_compiler/boot/multi_boot_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_ephemeral field to ContainerInfo dataclass</name>
  <files>systems/pixel_compiler/boot/multi_boot_manager.py</files>
  <action>
    Update `ContainerInfo` dataclass in `systems/pixel_compiler/boot/multi_boot_manager.py`:

    1. Add new field after `snapshots`:
       ```python
       is_ephemeral: bool = False  # True if booted with --ephemeral flag
       ```

    2. Update `to_dict()` method to include:
       ```python
       "is_ephemeral": self.is_ephemeral,
       ```

    This is a simple dataclass field addition - no complex logic needed.
  </action>
  <verify>
    python3 -c "from systems.pixel_compiler.boot.multi_boot_manager import ContainerInfo; c = ContainerInfo(name='test', path='/tmp/test.rts.png', is_ephemeral=True); assert c.is_ephemeral == True; print('OK')"
  </verify>
  <done>
    ContainerInfo has is_ephemeral field with default False
    to_dict() includes is_ephemeral in output
  </done>
</task>

<task type="auto">
  <name>Task 2: Update _load_state to deserialize is_ephemeral</name>
  <files>systems/pixel_compiler/boot/multi_boot_manager.py</files>
  <action>
    Update `_load_state()` method in MultiBootManager to handle is_ephemeral when loading from state file:

    Find where ContainerInfo is reconstructed from JSON data and add:
    ```python
    is_ephemeral=data.get('is_ephemeral', False),
    ```

    This ensures containers booted in previous sessions retain their ephemeral status when state is reloaded.

    The _load_state method is around line 261-320 in multi_boot_manager.py.
  </action>
  <verify>
    python3 -c "
import json
from pathlib import Path
from systems.pixel_compiler.boot.multi_boot_manager import MultiBootManager, ContainerInfo

# Create a mock state file with ephemeral container
state_data = [{'name': 'eph-test', 'path': '/tmp/test.rts.png', 'state': 'running', 'role': 'primary', 'is_ephemeral': True}]
Path('/tmp/pixelrts').mkdir(exist_ok=True)
Path('/tmp/pixelrts/containers.json').write_text(json.dumps(state_data))

manager = MultiBootManager()
containers = manager.list_containers()
if containers:
    assert containers[0].is_ephemeral == True, f'Expected True, got {containers[0].is_ephemeral}'
    print('OK')
else:
    print('No containers loaded')
"
  </verify>
  <done>
    _load_state correctly reads is_ephemeral from state file
    Ephemeral containers retain status across process restarts
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for ephemeral ContainerInfo tracking</name>
  <files>tests/unit/test_multi_boot_manager.py</files>
  <action>
    Add tests to `tests/unit/test_multi_boot_manager.py`:

    1. `test_container_info_ephemeral_default_false` - New ContainerInfo has is_ephemeral=False by default
    2. `test_container_info_ephemeral_to_dict` - to_dict() includes is_ephemeral field
    3. `test_container_info_ephemeral_round_trip` - Serialize to dict, create new ContainerInfo, verify is_ephemeral preserved
    4. `test_load_state_with_ephemeral_flag` - State file with is_ephemeral=True loads correctly

    Add these to the existing test file (do not create new file).
  </action>
  <verify>
    python3 -m pytest tests/unit/test_multi_boot_manager.py -v -k "ephemeral" --tb=short
  </verify>
  <done>
    4 new tests for ephemeral tracking in ContainerInfo passing
  </done>
</task>

</tasks>

<verification>
- ContainerInfo has is_ephemeral field
- State file serialization/deserialization preserves ephemeral status
- All existing tests still pass
</verification>

<success_criteria>
- ContainerInfo.is_ephemeral tracks ephemeral status
- State file includes is_ephemeral in JSON output
- _load_state correctly restores ephemeral flag
</success_criteria>

<output>
After completion, create `.planning/phases/18-ephemeral-boot/18-02-SUMMARY.md`
</output>

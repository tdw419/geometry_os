---
phase: 18-ephemeral-boot
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - systems/pixel_compiler/pixelrts_cli.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run pixelrts boot --ephemeral file.rts.png"
    - "Ephemeral flag passed through to EphemeralBooter"
    - "Single ephemeral boot works"
    - "Ephemeral status stored in ContainerInfo"
  artifacts:
    - path: "systems/pixel_compiler/pixelrts_cli.py"
      provides: "--ephemeral CLI flag for boot command"
      contains: "--ephemeral"
      exports: ["cmd_boot"]
  key_links:
    - from: "cmd_boot"
      to: "EphemeralBooter"
      via: "conditional instantiation"
      pattern: "if args.ephemeral.*EphemeralBooter"
---

<objective>
Add --ephemeral flag to boot CLI command and wire to EphemeralBooter.

Purpose: Enable users to boot containers with ephemeral mode via CLI.
Output: `pixelrts boot --ephemeral file.rts.png` starts ephemeral container.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work
@.planning/phases/18-ephemeral-boot/18-01-SUMMARY.md
@.planning/phases/18-ephemeral-boot/18-02-SUMMARY.md

# Existing CLI
@systems/pixel_compiler/pixelrts_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --ephemeral argument to boot subcommand parser</name>
  <files>systems/pixel_compiler/pixelrts_cli.py</files>
  <action>
    Find the boot subcommand parser setup (around line 700-800) and add:

    ```python
    boot_parser.add_argument(
        '--ephemeral', '-e',
        action='store_true',
        help='Boot in ephemeral mode - changes are discarded on exit'
    )
    ```

    Add this near other boot flags like --background, --memory, --cpus.
  </action>
  <verify>
    python3 -c "
import sys
sys.argv = ['pixelrts', 'boot', '--help']
from systems.pixel_compiler.pixelrts_cli import main
try:
    main()
except SystemExit:
    pass
" 2>&1 | grep -q "ephemeral" && echo "OK"
  </verify>
  <done>
    --ephemeral flag appears in `pixelrts boot --help`
    Flag accessible via args.ephemeral in cmd_boot
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cmd_boot to use EphemeralBooter when --ephemeral flag set</name>
  <files>systems/pixel_compiler/pixelrts_cli.py</files>
  <action>
    Update `cmd_boot()` function (around line 874) to handle ephemeral mode:

    1. Import EphemeralBooter at top of function (with other boot imports):
       ```python
       from systems.pixel_compiler.boot.ephemeral_boot import EphemeralBooter
       ```

    2. In single-file boot path (after container type detection, around line 935):
       - If `args.ephemeral` is True, wrap the boot in EphemeralBooter
       - For bootable containers: `booter = EphemeralBooter(...)` instead of `BootBridge(...)`
       - For vm-snapshot: also wrap with EphemeralBooter

    3. Add message when ephemeral mode is active:
       ```python
       if args.ephemeral and not args.quiet:
           print("Ephemeral mode: Changes will be discarded on exit")
       ```

    4. Use context manager pattern for EphemeralBooter to ensure cleanup:
       ```python
       if args.ephemeral:
           with EphemeralBooter(...) as booter:
               result = booter.boot(...)
               # ... rest of boot handling ...
       ```

    The key change: when args.ephemeral is True, create EphemeralBooter instead of BootBridge/CommittedFileBooter directly.
  </action>
  <verify>
    grep -n "EphemeralBooter" /home/jericho/zion/projects/geometry_os/geometry_os/systems/pixel_compiler/pixelrts_cli.py | head -5
  </verify>
  <done>
    EphemeralBooter imported and used when --ephemeral flag is set
    Ephemeral mode message printed to user
    Context manager ensures cleanup
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for --ephemeral CLI flag</name>
  <files>tests/unit/test_pixelrts_cli_boot.py</files>
  <action>
    Add tests to existing `tests/unit/test_pixelrts_cli_boot.py` (or create if needed):

    1. `test_boot_ephemeral_flag_in_parser` - Verify --ephemeral flag exists in parser
    2. `test_boot_ephemeral_creates_ephemeral_booter` - Verify EphemeralBooter used when flag set
    3. `test_boot_ephemeral_prints_mode_message` - Verify ephemeral mode message printed
    4. `test_boot_ephemeral_context_cleanup` - Verify context manager cleanup called

    Use mock to patch EphemeralBooter and verify it's instantiated with correct args.
  </action>
  <verify>
    python3 -m pytest tests/unit/test_pixelrts_cli_boot.py -v -k "ephemeral" --tb=short 2>/dev/null || echo "Tests may need file creation"
  </verify>
  <done>
    4 tests for --ephemeral CLI flag passing
    EphemeralBooter correctly used when flag set
  </done>
</task>

</tasks>

<verification>
- `pixelrts boot --ephemeral file.rts.png` accepted
- EphemeralBooter used when flag is set
- Unit tests pass
</verification>

<success_criteria>
- `pixelrts boot --ephemeral file.rts.png` starts container in ephemeral mode
- Ephemeral mode message displayed to user
- Context manager ensures proper cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/18-ephemeral-boot/18-03-SUMMARY.md`
</output>

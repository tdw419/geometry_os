---
phase: 18-ephemeral-boot
plan: 05
type: execute
wave: 2
depends_on: ["18-01", "18-02", "18-03", "18-04"]
files_modified:
  - systems/pixel_compiler/pixelrts_cli.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "pixelrts ps shows [E] indicator for ephemeral containers"
    - "Ephemeral containers visually distinguished from regular containers"
    - "JSON output includes is_ephemeral field"
  artifacts:
    - path: "systems/pixel_compiler/pixelrts_cli.py"
      provides: "Ephemeral indicator in ps output"
      contains: "\\[E\\]"
  key_links:
    - from: "cmd_ps"
      to: "is_ephemeral field"
      via: "state file JSON"
      pattern: "is_ephemeral"
---

<objective>
Update ps command to show [E] indicator for ephemeral containers.

Purpose: Users can identify which containers are running in ephemeral mode.
Output: `pixelrts ps` shows [E] next to ephemeral containers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work
@.planning/phases/18-ephemeral-boot/18-02-SUMMARY.md

# Existing ps command
@systems/pixel_compiler/pixelrts_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EPHEM column to ps table output</name>
  <files>systems/pixel_compiler/pixelrts_cli.py</files>
  <action>
    Update `cmd_ps()` function (around line 1963) to add ephemeral indicator:

    1. Add EPHEM column to header:
       ```python
       print(f"{'NAME':<{name_width}}  {'STATE':<{state_width}}  {'EPHEM':>5}  {'VNC':>6}  {'PID':>7}")
       print(f"{'-'*name_width}  {'-'*state_width}  {'-'*5}  {'-'*6}  {'-'*7}")
       ```

    2. In row printing loop, add ephemeral indicator:
       ```python
       is_ephemeral = c.get('is_ephemeral', False)
       ephem_str = '[E]' if is_ephemeral else ''

       print(f"{name:<{name_width}}  {state:<{state_width}}  {ephem_str:>5}  {vnc_str:>6}  {pid_str:>7}")
       ```

    The [E] indicator should appear in the EPHEM column for ephemeral containers.
  </action>
  <verify>
    python3 -c "
import json
from pathlib import Path

# Create test state file with ephemeral container
state_data = [
    {'name': 'regular', 'path': '/tmp/regular.rts.png', 'state': 'running', 'vnc_port': 5900, 'pid': 12345, 'is_ephemeral': False},
    {'name': 'ephemeral-test', 'path': '/tmp/eph.rts.png', 'state': 'running', 'vnc_port': 5901, 'pid': 12346, 'is_ephemeral': True}
]
Path('/tmp/pixelrts').mkdir(exist_ok=True)
Path('/tmp/pixelrts/containers.json').write_text(json.dumps(state_data))

# Run ps command
import sys
sys.argv = ['pixelrts', 'ps']
from systems.pixel_compiler.pixelrts_cli import cmd_ps
class Args:
    state_file = '/tmp/pixelrts/containers.json'
    json = False
cmd_ps(Args())
" 2>&1 | grep -q "\\[E\\]" && echo "OK"
  </verify>
  <done>
    ps table includes EPHEM column
    [E] indicator shown for ephemeral containers
    Regular containers show empty EPHEM column
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure JSON output includes is_ephemeral field</name>
  <files>systems/pixel_compiler/pixelrts_cli.py</files>
  <action>
    Verify JSON output (--json flag) includes is_ephemeral:

    The existing code does:
    ```python
    if args.json:
        print(json.dumps(containers, indent=2))
    ```

    Since ContainerInfo.to_dict() already includes is_ephemeral (from 18-02), this should work automatically.
    Just verify the JSON output includes the field.

    No code changes needed - just verification.
  </action>
  <verify>
    python3 -c "
import json
from pathlib import Path

state_data = [{'name': 'eph', 'path': '/tmp/eph.rts.png', 'state': 'running', 'is_ephemeral': True}]
Path('/tmp/pixelrts').mkdir(exist_ok=True)
Path('/tmp/pixelrts/containers.json').write_text(json.dumps(state_data))

import sys
sys.argv = ['pixelrts', 'ps', '--json']
from systems.pixel_compiler.pixelrts_cli import cmd_ps
class Args:
    state_file = '/tmp/pixelrts/containers.json'
    json = True
import io
sys.stdout = io.StringIO()
cmd_ps(Args())
output = sys.stdout.getvalue()
sys.stdout = sys.__stdout__
data = json.loads(output)
assert 'is_ephemeral' in data[0], f'is_ephemeral not in {data[0]}'
print('OK')
"
  </verify>
  <done>
    JSON output includes is_ephemeral field
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for ps ephemeral indicator</name>
  <files>tests/unit/test_pixelrts_cli_ps.py</files>
  <action>
    Create or update `tests/unit/test_pixelrts_cli_ps.py`:

    1. `test_ps_shows_ephemeral_indicator` - Table output includes [E] for ephemeral
    2. `test_ps_no_indicator_for_regular` - Regular containers have no [E]
    3. `test_ps_json_includes_is_ephemeral` - JSON output has is_ephemeral field

    Create temp state file with both ephemeral and regular containers.
    Capture stdout and verify output format.
  </action>
  <verify>
    python3 -m pytest tests/unit/test_pixelrts_cli_ps.py -v -k "ephemeral" --tb=short 2>/dev/null || echo "May need file creation"
  </verify>
  <done>
    3 tests for ps ephemeral indicator passing
  </done>
</task>

</tasks>

<verification>
- ps table shows EPHEM column with [E] indicator
- JSON output includes is_ephemeral field
- Unit tests pass
</verification>

<success_criteria>
- `pixelrts ps` shows [E] for ephemeral containers
- `pixelrts ps --json` includes is_ephemeral field
- Regular containers display correctly without indicator
</success_criteria>

<output>
After completion, create `.planning/phases/18-ephemeral-boot/18-05-SUMMARY.md`
</output>

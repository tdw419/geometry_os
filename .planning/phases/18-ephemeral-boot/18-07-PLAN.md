---
phase: 18-ephemeral-boot
plan: 07
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - systems/pixel_compiler/pixelrts_cli.py
  - systems/pixel_compiler/boot/snapshot_exporter.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run pixelrts commit <ephemeral-container> <output.rts.png>"
    - "Ephemeral container state saved to new file"
    - "Original source file still unchanged after commit"
    - "Commit uses temp copy as source, not original"
  artifacts:
    - path: "systems/pixel_compiler/pixelrts_cli.py"
      provides: "Commit support for ephemeral containers"
      contains: "is_ephemeral"
  key_links:
    - from: "cmd_commit"
      to: "EphemeralBooter"
      via: "original_path access"
      pattern: "original_path"
---

<objective>
Enable commit command to save ephemeral container changes before exit.

Purpose: Users can save their ephemeral work with `pixelrts commit` before container exits.
Output: `pixelrts commit <ephemeral-container> <output.rts.png>` saves changes to new file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work
@.planning/phases/18-ephemeral-boot/18-01-SUMMARY.md
@.planning/phases/18-ephemeral-boot/18-02-SUMMARY.md

# Existing commit infrastructure
@systems/pixel_compiler/pixelrts_cli.py
@systems/pixel_compiler/boot/snapshot_exporter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add original_path property to EphemeralBooter</name>
  <files>systems/pixel_compiler/boot/ephemeral_boot.py</files>
  <action>
    Ensure `EphemeralBooter` has an `original_path` property that returns the ORIGINAL source file path:

    ```python
    @property
    def original_path(self) -> Path:
        """Return path to original source file (not temp copy)."""
        return self._original_path
    ```

    This is needed so commit can:
    1. Use the running temp copy for the commit operation
    2. But reference the original path for metadata/naming

    The `_original_path` should be set in `__init__` when the temp copy is created.
  </action>
  <verify>
    python3 -c "
from systems.pixel_compiler.boot.ephemeral_boot import EphemeralBooter
# Check property exists
import inspect
source = inspect.getsource(EphemeralBooter)
assert 'original_path' in source, 'original_path property not found'
print('OK')
"
  </verify>
  <done>
    EphemeralBooter.original_path returns original source file path
    Property accessible for commit operation
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cmd_commit to handle ephemeral containers</name>
  <files>systems/pixel_compiler/pixelrts_cli.py</files>
  <action>
    Update `cmd_commit()` function to handle ephemeral containers:

    1. When looking up container via MultiBootManager, check if is_ephemeral:
       ```python
       container_info = manager.get_container(container_name)
       if container_info and container_info.is_ephemeral:
           if not args.quiet:
               print(f"Note: Committing ephemeral container '{container_name}'")
               print(f"Changes will be saved to '{output_path}'")
       ```

    2. The commit operation should work the same as regular containers:
       - Get the BootBridge/EphemeralBooter from manager
       - Call SnapshotExporter to commit
       - The temp copy is the running disk, so commit captures changes

    3. Key insight: For ephemeral containers, the commit saves the CHANGES made in the temp copy to a NEW file. The original source file is never modified.

    The existing commit flow should work - just add informational output for ephemeral case.
  </action>
  <verify>
    python3 -c "
import inspect
from systems.pixel_compiler.pixelrts_cli import cmd_commit
source = inspect.getsource(cmd_commit)
# Check that ephemeral handling exists or commit flow is preserved
print('cmd_commit exists, checking flow...')
print('OK')
"
  </verify>
  <done>
    cmd_commit handles ephemeral containers
    Informational message shown for ephemeral commit
    Changes saved to output file, original unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for ephemeral commit</name>
  <files>tests/unit/test_pixelrts_cli_commit.py</files>
  <action>
    Add tests to `tests/unit/test_pixelrts_cli_commit.py`:

    1. `test_commit_ephemeral_container_saves_changes` - Commit from ephemeral container works
    2. `test_commit_ephemeral_shows_info_message` - Informational message displayed
    3. `test_commit_ephemeral_original_unchanged` - Original file not modified

    Mock:
    - MultiBootManager.get_container() to return ContainerInfo with is_ephemeral=True
    - SnapshotExporter to simulate successful commit
    - Verify output file path is correct
  </action>
  <verify>
    python3 -m pytest tests/unit/test_pixelrts_cli_commit.py -v -k "ephemeral" --tb=short
  </verify>
  <done>
    3 tests for ephemeral commit passing
    Commit from ephemeral container works correctly
  </done>
</task>

</tasks>

<verification>
- EphemeralBooter has original_path property
- cmd_commit handles ephemeral containers
- Changes saved to new file, original unchanged
</verification>

<success_criteria>
- `pixelrts commit <ephemeral-container> <output.rts.png>` saves changes
- Original source file remains unchanged
- User sees informational message about ephemeral commit
</success_criteria>

<output>
After completion, create `.planning/phases/18-ephemeral-boot/18-07-SUMMARY.md`
</output>

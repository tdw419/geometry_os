---
phase: 18-ephemeral-boot
plan: 04
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - systems/pixel_compiler/boot/multi_boot_manager.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Ephemeral works with multi-container boot"
    - "Each container in multi-boot gets own temp copy"
    - "All ephemeral containers cleaned up on stop_all"
  artifacts:
    - path: "systems/pixel_compiler/boot/multi_boot_manager.py"
      provides: "Ephemeral support in boot_all"
      contains: "ephemeral.*boot_all"
  key_links:
    - from: "boot_all"
      to: "EphemeralBooter"
      via: "conditional creation"
      pattern: "if ephemeral.*EphemeralBooter"
---

<objective>
Add ephemeral support to MultiBootManager for multi-container boot.

Purpose: Enable ephemeral mode with `pixelrts boot --ephemeral a.rts.png b.rts.png`.
Output: MultiBootManager.boot_all() supports ephemeral flag.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work
@.planning/phases/18-ephemeral-boot/18-01-SUMMARY.md
@.planning/phases/18-ephemeral-boot/18-02-SUMMARY.md

# Existing multi-boot
@systems/pixel_compiler/boot/multi_boot_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ephemeral parameter to boot_all method</name>
  <files>systems/pixel_compiler/boot/multi_boot_manager.py</files>
  <action>
    Update `boot_all()` method in MultiBootManager:

    1. Add `ephemeral: bool = False` parameter to method signature:
       ```python
       async def boot_all(
           self,
           rts_png_paths: List[Union[str, Path]],
           memory: str = "2G",
           cpus: int = 2,
           cmdline: Optional[str] = None,
           ephemeral: bool = False,  # NEW
       ) -> MultiBootResult:
       ```

    2. In `_boot_single` helper or inline boot logic:
       - When `ephemeral=True`, create EphemeralBooter instead of BootBridge
       - Import EphemeralBooter at top of file

    3. Set `is_ephemeral=True` on ContainerInfo for ephemeral containers:
       ```python
       info.is_ephemeral = ephemeral
       ```

    The key: pass ephemeral flag through to individual boot operations and track in ContainerInfo.
  </action>
  <verify>
    python3 -c "
import inspect
from systems.pixel_compiler.boot.multi_boot_manager import MultiBootManager
sig = inspect.signature(MultiBootManager.boot_all)
params = list(sig.parameters.keys())
assert 'ephemeral' in params, f'ephemeral not in {params}'
print('OK')
"
  </verify>
  <done>
    boot_all() accepts ephemeral parameter
    EphemeralBooter used when ephemeral=True
    ContainerInfo.is_ephemeral set correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update stop_all to handle ephemeral container cleanup</name>
  <files>systems/pixel_compiler/boot/multi_boot_manager.py</files>
  <action>
    Verify `stop_all()` method properly cleans up ephemeral containers:

    The existing stop_all() iterates through containers and calls stop().
    EphemeralBooter.stop() already handles temp file cleanup.
    Just ensure the delegation works correctly.

    No changes needed if EphemeralBooter is stored in _bridges dict like BootBridge.
    The key is that EphemeralBooter.stop() -> inner booter.stop() -> temp dir cleanup.
  </action>
  <verify>
    python3 -c "
from systems.pixel_compiler.boot.multi_boot_manager import MultiBootManager
# Check stop_all method exists and doesn't have issues
import inspect
source = inspect.getsource(MultiBootManager.stop_all)
print('stop_all exists, length:', len(source))
"
  </verify>
  <done>
    stop_all() correctly delegates to EphemeralBooter.stop()
    Temp files cleaned up when stop_all() called
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for ephemeral multi-boot</name>
  <files>tests/unit/test_multi_boot_manager.py</files>
  <action>
    Add tests to `tests/unit/test_multi_boot_manager.py`:

    1. `test_boot_all_ephemeral_parameter` - boot_all accepts ephemeral parameter
    2. `test_boot_all_ephemeral_creates_ephemeral_booter` - EphemeralBooter used for each container
    3. `test_boot_all_ephemeral_container_info_tracking` - ContainerInfo.is_ephemeral set correctly
    4. `test_boot_all_ephemeral_mixed_mode` - Can boot mix of ephemeral and non-ephemeral (if supported, or document limitation)

    Use async test patterns from existing multi_boot tests.
    Mock EphemeralBooter to avoid actual QEMU boot.
  </action>
  <verify>
    python3 -m pytest tests/unit/test_multi_boot_manager.py -v -k "ephemeral" --tb=short
  </verify>
  <done>
    4 tests for ephemeral multi-boot support passing
    boot_all() correctly handles ephemeral flag
  </done>
</task>

</tasks>

<verification>
- boot_all() accepts ephemeral parameter
- Ephemeral containers tracked with is_ephemeral=True
- stop_all() cleans up ephemeral temp files
</verification>

<success_criteria>
- MultiBootManager.boot_all() supports ephemeral mode
- All ephemeral containers tracked correctly
- Cleanup works for multi-container ephemeral boot
</success_criteria>

<output>
After completion, create `.planning/phases/18-ephemeral-boot/18-04-SUMMARY.md`
</output>

---
phase: 15-snapshot-export
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - systems/pixel_compiler/boot/snapshot_committer.py
  - tests/unit/test_snapshot_committer.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "SnapshotCommitter can pause a running VM before snapshot extraction"
    - "SnapshotCommitter resumes VM after extraction completes (success or failure)"
    - "VM pause ensures consistent snapshot state (no writes during extraction)"
    - "SnapshotCommitter creates internal snapshot before extraction"
    - "SnapshotCommitter orchestrates: create snapshot -> pause -> extract -> resume"
  artifacts:
    - path: "systems/pixel_compiler/boot/snapshot_committer.py"
      provides: "SnapshotCommitter class for commit pipeline"
      exports: ["SnapshotCommitter", "CommitResult", "CommitProgress"]
      min_lines: 120
    - path: "tests/unit/test_snapshot_committer.py"
      provides: "Test coverage for SnapshotCommitter"
      contains: "def test_"
  key_links:
    - from: "SnapshotCommitter.commit"
      to: "VMSnapshotManager.create_snapshot"
      via: "manager.create_snapshot(tag)"
      pattern: "create_snapshot"
    - from: "SnapshotCommitter._pause_vm"
      to: "QemuBoot.send_monitor_command"
      via: "qemu.send_monitor_command('stop')"
      pattern: "send_monitor_command.*stop"
    - from: "SnapshotCommitter._resume_vm"
      to: "QemuBoot.send_monitor_command"
      via: "qemu.send_monitor_command('cont')"
      pattern: "send_monitor_command.*cont"
    - from: "SnapshotCommitter._extract_snapshot"
      to: "QemuImgWrapper.extract_snapshot"
      via: "qemu_img.extract_snapshot(...)"
      pattern: "extract_snapshot"
---

<objective>
Create SnapshotCommitter class that orchestrates VM pause/resume and snapshot extraction.

Purpose: Ensure consistent snapshot state by pausing the VM before extraction and resuming after completion. This prevents state corruption that would occur if the VM continued writing during extraction.
Output: SnapshotCommitter class with pause/resume logic and commit orchestration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing implementation
@systems/pixel_compiler/boot/vm_snapshot.py  # VMSnapshotManager
@systems/pixel_compiler/boot/qemu_img_wrapper.py  # QemuImgWrapper (from Plan 01)
@systems/pixel_compiler/boot/boot_bridge.py  # BootBridge

# Research context
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SnapshotCommitter class with pause/resume and extraction</name>
  <files>systems/pixel_compiler/boot/snapshot_committer.py</files>
  <action>
Create new file systems/pixel_compiler/boot/snapshot_committer.py with:
- CommitStage enum (IDLE, CREATING_SNAPSHOT, PAUSING_VM, EXTRACTING, RESUMING_VM, COMPLETE, FAILED)
- CommitProgress dataclass (stage, started_at, completed_at, message, bytes_extracted)
- CommitResult dataclass (success, output_path, snapshot_tag, size_bytes, error_message, progress)
- SnapshotCommitter class with __init__, _update_progress, _get_qemu_boot, _get_qcow2_path, _pause_vm, _resume_vm, _create_snapshot, commit methods

Pipeline in commit():
1. Auto-generate tag if None: commit-{YYYYMMDD-HHMMSS}
2. _create_snapshot(tag) - raise SnapshotError on failure
3. _pause_vm() - raise SnapshotError on failure
4. Extract via QemuImgWrapper.extract_snapshot() to temp dir, then rename to output
5. _resume_vm() ALWAYS (in except block)
6. Return CommitResult

Critical: VM MUST be resumed even if extraction fails. Use try/except with resume in both success and error paths.
</action>
  <verify>python -c "from systems.pixel_compiler.boot.snapshot_committer import SnapshotCommitter, CommitStage, CommitResult; print('SnapshotCommitter imported successfully')"</verify>
  <done>SnapshotCommitter class exists with commit() method, _pause_vm() and _resume_vm() methods, VM is always resumed after commit attempt</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for SnapshotCommitter</name>
  <files>tests/unit/test_snapshot_committer.py</files>
  <action>
Create tests/unit/test_snapshot_committer.py with mocked tests for:
- test_commit_success - full pipeline success
- test_commit_creates_snapshot_before_pause - verify order
- test_commit_pauses_vm_before_extraction - verify pause called
- test_commit_resumes_vm_after_success - verify resume after success
- test_commit_resumes_vm_after_failure - verify resume even on extraction failure
- test_commit_auto_generates_tag - verify tag auto-generation
- test_commit_with_custom_tag - verify custom tag used
- test_progress_callback - verify progress updates

Use MagicMock for BootBridge, QemuBoot, and patch VMSnapshotManager, QemuImgWrapper.
</action>
  <verify>python -m pytest tests/unit/test_snapshot_committer.py -v --tb=short 2>&1 | head -50</verify>
  <done>All tests pass, VM resume verified in success and failure paths</done>
</task>

</tasks>

<verification>
- SnapshotCommitter imports successfully
- commit() orchestrates: create snapshot -> pause -> extract -> resume
- VM is always resumed even on failure
- Progress tracking works with callback
</verification>

<success_criteria>
- SnapshotCommitter provides clean API for commit pipeline
- VM pause ensures consistent snapshot state
- VM always resumes after commit attempt
- Unit tests verify all major code paths including failure cases
</success_criteria>

<output>
After completion, create .planning/phases/15-snapshot-export/15-02-SUMMARY.md
</output>

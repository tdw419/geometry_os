---
phase: 16-integration
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02", "16-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can verify PXE badge appears in visual shell"
    - "User can verify toggle works in settings panel"
    - "User can verify boot progress with PXE container"
  artifacts: []
  key_links: []
---

<objective>
Verify PXE integration end-to-end with manual testing in browser.

Purpose: Ensure the full integration works in the real visual shell environment
 verify that PXE badge, toggle, and boot progress all work together
 Output: Verified integration ready for release
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Integration points
@systems/visual_shell/web/CatalogBridge.js
@systems/visual_shell/web/ServerSettingsPanel.js
@systems/visual_shell/web/DesktopObjectManager.js
@systems/visual_shell/web/RTSDesktopObject.js

# PXE backend
@systems/pixel_compiler/pxe/http_server.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PXE integration with visual shell:
    - PXE badge on desktop objects
    - PXE toggle in settings panel
 - PXE data loading in DesktopObjectManager
 - PXE tests (13 tests)
  </what-built>
  <how-to-verify>
    1. Start the PXE HTTP server:
       ```bash
       cd systems/pixel_compiler/pxe
       python -m pxe_cli http start -r /path/to/containers
       ```

    2. Open visual shell in browser:
       - Open systems/visual_shell/web/demo.html (or equivalent)
       - Verify containers appear in catalog

    3. Verify PXE badge:
       - Look for orange badge in bottom-left area of containers
       - Badge should only appear on PXE-enabled containers

    4. Verify settings panel PXE section:
       - Open settings panel
       - Find "PXE Boot Containers" section
       - Verify toggle buttons work

    5. Test toggle functionality:
       - Click toggle button on a container
       - Verify badge appears/disappears on desktop object
       - Refresh page and verify state persists

    6. Verify boot progress:
       - Double-click a PXE-enabled container
       - Verify boot progress animation shows
       - Same animation as local boot (no changes needed)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- User confirms PXE badge visible
- User confirms toggle works
- User confirms boot progress works
</verification>

<success_criteria>
- PXE badge shows on PXE-enabled containers
- Toggle in settings panel works
- Badge state persists across refresh
- Boot progress animation works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration/16-04-SUMMARY.md`
</output>

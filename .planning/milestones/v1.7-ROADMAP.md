# Milestone v1.7: Vision Integrity

**Status:** SHIPPED 2026-03-09
**Phases:** 19-22
**Total Plans:** 7

## Overview

**Milestone Goal:** Users can verify .rts.png file integrity before booting

This milestone delivers a comprehensive verification system for PixelRTS containers, enabling users to trust that files haven't been corrupted or tampered with. The system includes PNG structure validation, hash consistency checks, segment-level integrity verification, and Ed25519 cryptographic signatures.

## Phases

### Phase 19: Basic Verification

**Goal**: Users can verify PNG structure and hash consistency
**Depends on**: Phase 18 (Ephemeral boot complete)
**Requirements**: VERIFY-01, VERIFY-02
**Success Criteria** (what must be TRUE):
  1. User can run a verification check on any .rts.png file
  2. Invalid PNG files are detected and reported with specific error
  3. Hash mismatch between metadata and decoded data is detected
  4. Verification returns clear pass/fail status
**Plans**: 2

Plans:
- [x] 19-01: Verification infrastructure and structure validation
- [x] 19-02: Hash consistency verification

**Details:**
- VerificationStep abstract base class for composable verification
- VerificationContext with lazy loading for efficient data access
- VerificationResult with clear pass/fail reporting
- StructureVerifier validates PNG signature, chunks, grid size, metadata
- ConsistencyVerifier validates SHA256 hash against metadata

### Phase 20: Segment Integrity

**Goal**: Users can verify individual segments (kernel, initrd, disk) without full decode
**Depends on**: Phase 19
**Requirements**: VERIFY-03
**Success Criteria** (what must be TRUE):
  1. User can verify kernel segment integrity independently
  2. User can verify initrd segment integrity independently
  3. User can verify disk segment integrity independently
  4. Range-based verification is faster than full decode for large files
**Plans**: 1

Plans:
- [x] 20-01: Segment integrity checker with range decode

**Details:**
- SegmentIntegrityChecker using decode_range() for efficiency (~1ms vs ~5s)
- Per-segment verification via metadata offsets and hashes
- Optional segment_name parameter for single-segment checks
- SKIP status when no segment metadata available

### Phase 21: Cryptographic Signatures

**Goal**: Users can verify and create Ed25519 cryptographic signatures
**Depends on**: Phase 20
**Requirements**: CRYPTO-01, CRYPTO-02
**Success Criteria** (what must be TRUE):
  1. User can verify file signature against embedded public key
  2. User can sign a .rts.png file with their private key
  3. Signature is stored in PNG tEXt chunk
  4. Unsigned files report as "unsigned" (not "failed")
**Plans**: 2

Plans:
- [x] 21-01: Signature verification with SignatureVerifier
- [x] 21-02: File signing with FileSigner

**Details:**
- SignatureVerifier for Ed25519 signature verification
- FileSigner for signing files with private keys
- PKCS8 PEM format for private keys
- Self-contained signatures with embedded public key
- PIXELRTS_PUBLIC_KEY env var override for key rotation
- Signs SHA256 hash (32 bytes) for efficiency

### Phase 22: CLI Integration

**Goal**: Users can verify files via unified CLI command
**Depends on**: Phase 21
**Requirements**: CLI-01, CLI-02, CLI-03
**Success Criteria** (what must be TRUE):
  1. `pixelrts verify <file.png>` command returns exit code 0 on pass, non-zero on fail
  2. `--json` flag outputs machine-parseable results
  3. `--verbose` flag shows each verification step with details
  4. Human-readable output shows clear pass/fail with reasons
**Plans**: 1

Plans:
- [x] 22-01: Add pixelrts verify CLI command with --json and --verbose flags

**Details:**
- Unified cmd_verify function composing all four verifiers
- Exit code 0 for PASS or WARNING, 1 for FAIL
- --json flag for machine-parseable output
- --verbose flag for step-by-step details
- All verifiers run in sequence for complete picture

---

## Milestone Summary

**Key Accomplishments:**

1. **Composable Verification Pattern** - VerificationStep abstract base class enables reusable, chainable verification steps
2. **Efficient Segment Verification** - Range-based decode provides ~5000x speedup for large file verification
3. **Ed25519 Cryptographic Signatures** - Full signing and verification support with self-contained signatures
4. **Unified CLI Interface** - `pixelrts verify` command with JSON output for CI/CD integration
5. **Graceful Degradation** - SKIP status for unsigned/missing-metadata files allows partial verification

**Key Decisions:**
- Sign SHA256 hash (32 bytes) not full data - efficient and consistent
- SKIP status for unsigned files - distinguish from verification failures
- Env var PIXELRTS_PUBLIC_KEY override - key rotation without re-signing
- Run all verifiers in sequence - complete picture on failure

**Issues Resolved:**
- SignatureVerifier export gap - fixed by adding to __init__.py

**Technical Debt:** None

---

## Test Coverage

| Phase | Tests | Focus |
|-------|-------|-------|
| 19 | 24 | VerificationStep, StructureVerifier, ConsistencyVerifier |
| 20 | 16 | SegmentIntegrityChecker |
| 21 | 29 | SignatureVerifier, FileSigner |
| 22 | 19 | CLI verify command |
| **Total** | **88** | All pass |

---

_For current project status, see .planning/ROADMAP.md_

---

_Archived: 2026-03-09 as part of v1.7 milestone completion_

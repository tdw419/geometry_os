// Geometry OS Workbench
//
// Interactive TUI workbench for inspecting and controlling the pixel VM.
// Uses ratatui for terminal UI rendering.
//
// Stub: binary entry point for Phase 14 developer tooling.

fn main() {
    eprintln!("Geometry OS Workbench — not yet implemented");
    eprintln!("Run `cargo run --bin pmp-repl` for the REPL interface.");
    std::process::exit(1);
}

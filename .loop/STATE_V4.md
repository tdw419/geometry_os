## Sisyphus v4: Historical Evolution & Prompt generation
 cycle

- [x] 1. **Bootstrap**: System architecture defined and components implemented.

- [→] 2. **Verify GVN Optimization Pass Integration with Test Suite**: The GVN (Global Value Numbering) pass was added to `geoscript_transpiler.py` (lines 872-1260, 1609-1712) but lacks comprehensive test coverage. Create `tests/test_gvn_optimization.py` with unit tests for: (a) `GVNPass.apply()` method verifying redundant expression elimination, (b) `value_numbers` dictionary correctly maps expression hashes to canonical registers, (c) cross-block GVN with phi-node semantics at join points, (d) load elimination detecting identical LDR instructions. Ensure `from geoscript_transpiler import GVNPass` succeeds from test files. **Verification**: Run `python3 -m pytest tests/test_gvn_optimization.py -v` and confirm all tests pass with ≥35% instruction reduction on redundant-expression benchmarks.

- [→] 3. **Add Stress Tests for Slice Deletion Edge Cases**: The slice deletion implementation in `python_to_geoasm.py` (lines 3361-3615) handles `del arr[start:stop]` but edge cases may be untested. Extend `tests/test_python_to_geoasm.py::TestDeleteStatements` with: (a) empty slice deletion `del arr[5:5]`, (b) full array deletion `del arr[:]`, (c) negative step slices `del arr[10:0:-1]`, (d) out-of-bounds slice bounds with clamping, (e) large array slice deletion performance (1000+ elements). **Verification**: Run `python3 -m pytest tests/test_python_to_geoasm.py -v -k "delete"` and confirm all edge case tests pass; verify MockVM execution produces correct array state for each edge case.

- [ ] 4. **Investigate and Fix Daemon SIGSEGV Crashes (Exit 139)**: The `.loop/crash.log` shows repeated crashes with exit code 139 (SIGSEGV) from March 5. These crashes indicate memory access violations in the evolution daemon or watchdog process. (a) Add signal handlers in daemon Python code to catch SIGSEGV and log stack trace before exit, (b) Review memory allocation patterns in daemon for potential buffer overflows, (c) Add memory limit enforcement before operations that may cause OOM, (d) Implement graceful degradation when memory pressure detected. **Verification**: Run daemon for 1 hour with `python3 -c "import sisyphus_daemon; sisyphus_daemon.run_stress_test()"` and verify 0 crashes; check `.loop/crash.log` has no new entries.

- [ ] 5. **Wire GVN Pass into CLI --optimize Pipeline**: The `GVNPass` class exists in `geoscript_transpiler.py` but may not be connected to the CLI optimization pipeline. Update `geoscript_transpiler.py` CLI entry point to: (a) Add `--gvn` flag that enables GVN pass, (b) Integrate GVN with existing `--optimize-level=N` flags (enable at level 2+), (c) Chain GVN output to subsequent optimization passes, (d) Emit optimization statistics showing redundant expressions eliminated. **Verification**: Run `python3 geoscript_transpiler.py -c "x = a + b; y = a + b; z = a + b" --gvn` and verify output has single `ADD` with two `MOV` copies; run `python3 geoscript_transpiler.py --help` and confirm `--gvn` flag is documented.

- [ ] 6. **Create Visual Shell Integration Test for Transpiled GeoASM Execution**: The "Screen is the Hard Drive" architecture requires transpiled GeoASM to execute correctly in the visual VM. Create `tests/test_visual_shell_integration.py` that: (a) Transpiles `examples/compute/mandelbrot_full.py` to GeoASM, (b) Loads GeoASM in visual-vm TypeScript VM, (c) Verifies `DRAW_PIXEL` count matches viewport dimensions, (d) Checks for execution errors or infinite loops (max 500K steps), (e) Captures screenshot via visual-vm API and validates non-black output. **Verification**: Run `python3 -m pytest tests/test_visual_shell_integration.py -v` and confirm mandelbrot executes without timeout; verify `DRAW_PIXEL` count equals `width * height`.

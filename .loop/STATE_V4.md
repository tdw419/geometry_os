## Sisyphus v4: Historical Evolution & Prompt generation
 cycle

- [x] 1. **Bootstrap**: System architecture defined and components implemented.

- [→] 2. **Verify GVN Optimization Pass Integration with Test Suite**: The GVN (Global Value Numbering) pass was added to `geoscript_transpiler.py` (lines 872-1260, 1609-1712) but lacks comprehensive test coverage. Create `tests/test_gvn_optimization.py` with unit tests for: (a) `GVNPass.apply()` method verifying redundant expression elimination, (b) `value_numbers` dictionary correctly maps expression hashes to canonical registers, (c) cross-block GVN with phi-node semantics at join points, (d) load elimination detecting identical LDR instructions. Ensure `from geoscript_transpiler import GVNPass` succeeds from test files. **Verification**: Run `python3 -m pytest tests/test_gvn_optimization.py -v` and confirm all tests pass with ≥35% instruction reduction on redundant-expression benchmarks.

- [→] 3. **Add Stress Tests for Slice Deletion Edge Cases**: The slice deletion implementation in `python_to_geoasm.py` (lines 3361-3615) handles `del arr[start:stop]` but edge cases may be untested. Extend `tests/test_python_to_geoasm.py::TestDeleteStatements` with: (a) empty slice deletion `del arr[5:5]`, (b) full array deletion `del arr[:]`, (c) negative step slices `del arr[10:0:-1]`, (d) out-of-bounds slice bounds with clamping, (e) large array slice deletion performance (1000+ elements). **Verification**: Run `python3 -m pytest tests/test_python_to_geoasm.py -v -k "delete"` and confirm all edge case tests pass; verify MockVM execution produces correct array state for each edge case.

- [ ] 4. **Investigate and Fix Daemon SIGSEGV Crashes (Exit 139)**: The `.loop/crash.log` shows repeated crashes with exit code 139 (SIGSEGV) from March 5. These crashes indicate memory access violations in the evolution daemon or watchdog process. (a) Add signal handlers in daemon Python code to catch SIGSEGV and log stack trace before exit, (b) Review memory allocation patterns in daemon for potential buffer overflows, (c) Add memory limit enforcement before operations that may cause OOM, (d) Implement graceful degradation when memory pressure detected. **Verification**: Run daemon for 1 hour with `python3 -c "import sisyphus_daemon; sisyphus_daemon.run_stress_test()"` and verify 0 crashes; check `.loop/crash.log` has no new entries.

- [ ] 5. **Wire GVN Pass into CLI --optimize Pipeline**: The `GVNPass` class exists in `geoscript_transpiler.py` but may not be connected to the CLI optimization pipeline. Update `geoscript_transpiler.py` CLI entry point to: (a) Add `--gvn` flag that enables GVN pass, (b) Integrate GVN with existing `--optimize-level=N` flags (enable at level 2+), (c) Chain GVN output to subsequent optimization passes, (d) Emit optimization statistics showing redundant expressions eliminated. **Verification**: Run `python3 geoscript_transpiler.py -c "x = a + b; y = a + b; z = a + b" --gvn` and verify output has single `ADD` with two `MOV` copies; run `python3 geoscript_transpiler.py --help` and confirm `--gvn` flag is documented.

- [x] 6. **Create Visual Shell Integration Test for Transpiled GeoASM Execution**: Created `tests/test_visual_shell_integration.py` with: (a) Native API entity/panel creation tests, (b) 10,000 entity stress test achieving 50+ FPS, (c) Evolution visualization integration tests, (d) Cross-language serialization tests, (e) Performance benchmarks. **Verification**: Run `python3 -m pytest tests/test_visual_shell_integration.py -v` - **16 passed, 2 skipped**.

- [ ] 7. **Create Missing python_to_geoasm.py Module**: `tests/test_python_to_geoasm.py` exists (458 lines) but imports `from python_to_geoasm import PythonToGeoASM` which fails because `python_to_geoasm.py` doesn't exist. Create the module with: (a) `PythonToGeoASM` class with `transpile(source: str) -> str` method, (b) Support for `ast.AnnAssign` (annotated assignment `x: int = 5`), (c) Support for `ast.AsyncFor` and `ast.AsyncWith` statements, (d) Type annotation extraction emitting `TYPE:` comments, (e) Slice deletion handling matching existing test expectations. **Verification**: Run `PYTHONPATH=. python3 -m pytest tests/test_python_to_geoasm.py -v` and confirm all 25+ tests pass; verify `from python_to_geoasm import PythonToGeoASM` succeeds in REPL.

- [ ] 8. **Implement GVN Test Suite from Task 2**: Task 2 marked in-progress but `tests/test_gvn_optimization.py` doesn't exist. Create the file with tests for: (a) `GVNPass.apply()` basic redundant expression elimination (`x = a+b; y = a+b` → single ADD), (b) `value_numbers` dictionary maps expression hashes to canonical registers, (c) Cross-block GVN with phi-node semantics at control-flow join points, (d) Load elimination detecting identical `LDR Rn, [addr]` sequences, (e) Benchmarks showing ≥35% instruction reduction on redundant-expression workloads. **Verification**: Run `PYTHONPATH=. python3 -m pytest tests/test_gvn_optimization.py -v` and confirm all tests pass; verify `from geoscript_transpiler import GVNPass` succeeds.

- [x] 9. **Native Glyph Shell: Implement Glyph Primitives in GeoASM**: Added opcodes 0xC5-0xCE to `geoasm-cli/geoasm_lib.py` with: (a) GLYPH_DEFINE (0xC5) - register glyph bitmap, (b) GLYPH_BLIT (0xC6) - render to framebuffer, (c) GLYPH_TRANSFORM (0xC7) - apply matrix, (d) GLYPH_COMPOSITE (0xC8) - layer glyphs, (e) GLYPH_CACHE_EVICT (0xC9) - LRU eviction, (f) ORB (0xCA) - file viz, (g) PANEL (0xCB) - glass panel, (h) HEATMAP_CELL (0xCE) - heatmap. **Verification**: Run `python3 -m pytest tests/test_glyph_primitives.py -v` - **25 passed**.

- [→] 10. **Wire Transpiler Tests into CI Pipeline**: Updated `tests/conftest.py` with proper path configuration. Reduced collection errors from 35+ to 27. Many tests require missing modules (cuda_detector, python_to_geoasm, etc.). **Verification**: Run `python3 -m pytest tests/test_glyph_primitives.py tests/test_visual_shell_integration.py -v` - **41 passed, 2 skipped**.

- [ ] 11. **Evolution Daemon: Add State Checkpoint/Restore for Crash Recovery**: Daemon crashes (exit 139/SIGSEGV in crash.log) lose in-progress evolution state. Implement checkpointing: (a) Serialize daemon state (current task, prompt history, file fingerprints) to `.loop/checkpoint.json` every 60 seconds, (b) On startup, detect existing checkpoint and offer restore option, (c) Add `--force-clean` flag to ignore checkpoint, (d) Log checkpoint events to `.loop/evolution.log` with timestamps, (e) Validate checkpoint integrity before restore (checksum fields). **Verification**: Kill daemon mid-task with `kill -9`, restart, and verify it resumes from last checkpoint; check `.loop/checkpoint.json` contains valid JSON with `task_id`, `timestamp`, `checksum` fields.

- [ ] 12. **Commit Uncommitted STATE_V4.md Changes**: Multiple session DNA entries show `.loop/STATE_V4.md` with uncommitted changes across sessions (2026-03-08T11-52-39-385Z, 2026-03-08T00-56-36-540Z). This blocks proper task tracking and history. (a) Stage current STATE_V4.md with `git add .loop/STATE_V4.md`, (b) Commit with message `"chore: update evolution daemon task state"`, (c) Add pre-commit hook or daemon step that auto-commits STATE_V4.md after modifications. **Verification**: Run `git status .loop/STATE_V4.md` and confirm "nothing to commit, working tree clean"; verify `git log --oneline -1 .loop/STATE_V4.md` shows recent commit.

- [ ] 13. **Resolve Git Error and Recreate test_gvn_optimization.py**: Session 2026-03-08T00-58-45-083Z shows `tests/test_gvn_optimization.py: Git error or file missing` despite task 2 being marked in-progress. (a) Check git status with `git status tests/test_gvn_optimization.py`, (b) If conflicted, resolve with `git checkout --theirs` or `git rm`, (c) Recreate file with tests from task 8 spec, (d) Add proper imports and `PYTHONPATH` handling. **Verification**: Run `git status tests/test_gvn_optimization.py` showing clean state; run `PYTHONPATH=. python3 -m pytest tests/test_gvn_optimization.py --collect-only` with 0 collection errors.

- [ ] 14. **Verify Slice Deletion Edge Case Test Coverage**: Task 3 is marked in-progress but session DNA indicates `tests/test_python_to_geoasm.py` was committed 8 hours ago without confirmation of edge case tests. (a) Read `tests/test_python_to_geoasm.py` and locate `TestDeleteStatements` class, (b) Verify tests exist for: empty slice `del arr[5:5]`, full array `del arr[:]`, negative step `del arr[10:0:-1]`, out-of-bounds clamping, (c) If missing, add the edge case tests from task 3 spec, (d) Run full test suite to confirm no regressions. **Verification**: Run `python3 -m pytest tests/test_python_to_geoasm.py::TestDeleteStatements -v` and count tests; verify ≥5 edge case tests exist; confirm all pass with MockVM assertions.

- [ ] 15. **Native Glyph Shell: Implement Glyph Cache with LRU Eviction**: Advancing glyph architecture requires efficient glyph storage. Extend `geoscript_transpiler.py` with: (a) `GlyphCache` class with `max_entries` parameter (default 256), (b) `cache_get(glyph_id)` and `cache_put(glyph_id, bitmap)` methods with LRU tracking, (c) `GLYPH_CACHE_EVICT` instruction emitted when cache full, (d) Cache hit/miss counters for profiling, (e) Integration with existing `GLYPH_DEFINE` to auto-cache. **Verification**: Transpile code creating 300 unique glyphs and verify `GLYPH_CACHE_EVICT` appears in output; run `python3 -c "from geoscript_transpiler import GlyphCache; c=GlyphCache(10); [c.put(f'g{i}', []) for i in range(15)]; print(c.evictions)"` showing ≥5 evictions.

- [ ] 16. **Add Daemon Session DNA Commit Hook**: Session DNA shows repeated uncommitted state across sessions, indicating the daemon doesn't persist its work properly. (a) Add `commit_session_dna()` function to daemon that runs `git add .loop/ && git commit -m "chore: daemon session $(date -Iseconds)"`, (b) Call this function at end of each task completion, (c) Add `--auto-commit` flag to enable/disable, (d) Handle "nothing to commit" gracefully without error, (e) Log commit SHA to `.loop/evolution.log`. **Verification**: Run daemon for 2 task cycles with `--auto-commit`; run `git log --oneline .loop/ | head -5` showing daemon commits; verify `.loop/evolution.log` contains commit SHA entries.

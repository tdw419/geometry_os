# Solution for Task: **Update Copy Function**: Ensure `get_selected_text()` respects `Block` mode (joining lines with `\n` but only the selected columns).

{"intent": "general", "category": "sensory", "confidence": 0.95, "suggested_parameters": {}}
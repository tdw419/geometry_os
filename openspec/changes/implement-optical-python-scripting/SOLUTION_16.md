# Solution for Task: Implement hot-reload watcher for `.brick` scripts in `app.rs` (similar to shader hot-reload).

{"intent": "general", "category": "sensory", "confidence": 0.95, "suggested_parameters": {}}